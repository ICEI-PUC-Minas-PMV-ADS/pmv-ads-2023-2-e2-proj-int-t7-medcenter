@model IEnumerable<medcenter_backend.Models.Pedidos.Result>

@{
    ViewData["Title"] = "Listar";
}

<h1>Listar</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PedidosAbertos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProcedimentosAgendados)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProcedimentosRealizados)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PedidosAbertos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProcedimentosAgendados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProcedimentosRealizados)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
